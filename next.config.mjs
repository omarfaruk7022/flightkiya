/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["flightkiya.cosmelic.com"], // Just the domain without 'https://'
  },
  reactStrictMode: true,
};

export default nextConfig;
